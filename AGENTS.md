# AGENTS.md

## Cursor Cloud specific instructions

This is currently an empty repository with only a placeholder `README.md`. There are no applications, services, dependencies, tests, or build systems to run.

- **No package manager or dependency manifest** exists (`package.json`, `requirements.txt`, etc.).
- **No lint, test, or build commands** are available.
- **No services** need to be started.

When code is added to this repository, update this section with relevant development instructions.
